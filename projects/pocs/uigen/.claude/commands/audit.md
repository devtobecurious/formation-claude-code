Your goal is to update any vulnerable dependencies

do the following
1. run `npm audit` to find vulnerable installed package in this project
2. run `npm audit fix` to apply fixes
3. run tests and verify the updates didn't break anything
4. correct break
5. do step 3. and 4. until all tests are ok