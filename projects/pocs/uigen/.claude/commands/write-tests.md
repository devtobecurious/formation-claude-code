write comprehensive tests for : $ARGUMENTS

testing conventions
* use vitests with react testing library
* place test files in a __tests__ directory in the same directory as the source file
* name tests files as [filename].test/ts(x)
* use @/ prefix for imports

coverage:
* test happy path
* test edge case
* test error states
